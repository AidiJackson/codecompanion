--- /home/user/codecompanion/requirements.txt	(original)
+++ /home/user/codecompanion/requirements.txt	(optimized)
@@ -1,5 +1,32 @@
-rich>=13.7
+# Core Runtime Dependencies
+# HTTP client - modern, async-capable (replaces requests)
 httpx>=0.27
-pytest>=7.0
-ruff>=0.1
-pyright>=1.1
+
+# Web Framework
+fastapi>=0.115.0
+uvicorn[standard]>=0.30.0
+
+# Data Validation & Settings
+pydantic>=2.0.0
+pydantic-settings>=2.0.0
+
+# CLI Framework
+typer>=0.12.0
+
+# AI/LLM Clients
+anthropic>=0.40.0
+openai>=1.0.0
+google-generativeai>=0.8.0
+
+# UI Framework
+streamlit>=1.50.0
+
+# Data Processing & Visualization
+pandas>=2.0.0
+numpy>=1.24.0
+plotly>=5.18.0
+
+# Optional: Vector/ML Operations
+scikit-learn>=1.3.0  # For TF-IDF fallback in vector store

--- /home/user/codecompanion/pyproject.toml	(original)
+++ /home/user/codecompanion/pyproject.toml	(optimized)
@@ -7,7 +7,29 @@
 version = "0.1.0"
 description = "CodeCompanion launcher for Replit shells with built-in agent bootstrap"
 requires-python = ">=3.9"
-dependencies = ["rich>=13.7", "httpx>=0.27"]
+dependencies = [
+    "httpx>=0.28.0",
+    "fastapi>=0.115.0",
+    "uvicorn[standard]>=0.30.0",
+    "pydantic>=2.0.0",
+    "pydantic-settings>=2.0.0",
+    "typer>=0.12.0",
+    "anthropic>=0.40.0",
+    "openai>=1.0.0",
+    "google-generativeai>=0.8.0",
+    "streamlit>=1.50.0",
+    "pandas>=2.0.0",
+    "numpy>=1.24.0",
+    "plotly>=5.18.0",
+    "scikit-learn>=1.3.0",
+]
+
+[project.optional-dependencies]
+dev = [
+    "pytest>=8.0.0",
+    "pytest-asyncio>=0.23.0",
+    "pytest-cov>=4.1.0",
+    "ruff>=0.8.0",
+    "pyright>=1.1.380",
+    "ipython>=8.12.0",
+]

